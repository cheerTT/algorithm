package meituan.arr;

public class 有序数组找重复数字下标 {

    private int[] findArr(int[] nums, boolean isLeft) {

        int left = 0;
        int right = nums.length - 1;

        while (left <= right) {

            int mid = (left + right) / 2;

        }

        return null;
    }
}
